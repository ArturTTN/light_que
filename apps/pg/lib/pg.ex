defmodule Pg do
  @moduledoc """
  Documentation for Pg.
  """

  def get_all_jobs do
    []
  end

  def insert(term) do
    {:ok, %{}}
  end

  def delete(job_id) do
    :ok
  end

  def update(task_id, reason) do
    :ok
  end

end
